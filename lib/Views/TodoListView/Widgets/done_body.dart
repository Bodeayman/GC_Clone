import 'package:flutter/material.dart';

class DoneBody extends StatelessWidget {
  const DoneBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("This is the done page"),
      ],
    );
  }
}
