import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xff1f1b24);

final mobileWidth = WidgetsBinding.instance.window.physicalSize.width;
final mobileHeight = WidgetsBinding.instance.window.physicalSize.height;
